export const libraryConfig = {
  // Demo configuration - sets break limit to 30 seconds for quick viva demonstrations.
  // In a real environment, this would be 30 * 60 * 1000 (30 minutes).
  BREAK_DURATION: 30 * 1000, 
};
